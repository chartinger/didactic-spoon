ANKER_EMAIL=you@example.com
ANKER_PASSWORD=your-password
ANKER_COUNTRY_ID=DE
# Tokens are auto-cached to apitoken.cache.json after first login.
# These are only used as a fallback if no cache file exists.
ANKER_TOKEN=
ANKER_GTOKEN=
TARGET_BROKER=
TARGET_TOPIC=
